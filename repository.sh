# find the sources file in /etc/apt/sorces.list
sudo apt update
# add the repositoy either trought command 

# get the key with wget www. gg
# add the key with apt key add -
# pi , pe
add-apt-repository ppa: webupt8

apt-key list

# apt install opera stable as example 

# pp personal p acadie add the key and ad to the repository   need user as parameter 
# Step 1: Add the PPA
# Replace 'ppa:user/ppa-name' with the actual PPA you want to add
sudo add-apt-repository ppa:user/ppa-name -y

# Step 2: Update your package list
sudo apt update

# Step 3: Install the package
# Replace 'package-name' with the name of the package you want to install
sudo apt install package-name